<h1>Listing ownerships</h1>

<table>
  <thead>
    <tr>
      <th>Team</th>
      <th>Player</th>
      <th>Round</th>
      <th>Pick</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ownerships.each do |ownership| %>
      <tr>
        <td><%= ownership.team_id %></td>
        <td><%= ownership.player_id %></td>
        <td><%= ownership.round %></td>
        <td><%= ownership.pick %></td>
        <td><%= link_to 'Show', ownership %></td>
        <td><%= link_to 'Edit', edit_ownership_path(ownership) %></td>
        <td><%= link_to 'Destroy', ownership, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ownership', new_ownership_path %>
